# 2026-03-20

- Dylan要求：后续在这台机器上做封面图或配图生成时，默认使用 `wechat-publisher` skill。
- 纯图片生成优先运行：`python3 /home/openclaw/.openclaw/workspace/skills/wechat-publisher/jimeng_image.py ...`
- 后续只要是发布微信公众号，默认使用 `wechat-mp-publisher` skill：`python3 /home/openclaw/.openclaw/workspace/skills/wechat-mp-publisher/scripts/wechat_mp_publish.py ...`
- 发布公众号时如需即梦封面，允许由 `wechat-mp-publisher` 调用 `wechat-publisher/jimeng_image.py` 作为封面生成后端。
- 不要再用旧的 `wechat-publisher/publisher.py` 或 `publisher_enhanced.py` 做公众号发布，除非 Dylan 明确要求。
- 遇到图片生成失败时，先查看真实脚本输出和 HTTP 错误，不要猜测 provider 或密钥格式。
- 公众号稿件的 `阅读原文` 以后统一引流到 Dylan 的博客 canonical URL，而不是正文里的外站链接。
- 如果 Markdown 文章位于 `/home/openclaw/blog/posts/*.md`，优先让 `wechat-mp-publisher` 自动推导 `https://www.dylanslife.com/posts/<slug>.html`。
- 如果是 HN Top 5 这类公众号专用摘要稿，显式传 `--source-url` 指向当天对应的 HN 博客文章，例如 `https://www.dylanslife.com/posts/2026-03-20-hackernews-top50.html`。
- 今天新增了 ClawTeam 协作层，用于 role-based agent team。
- 默认 team：`dev-swarm`
- 默认内部 leader mailbox：`butler-hub`，后续由 `butler` worker 主动消费
- 默认角色：`butler`、`writing-master`、`system-architect`、`implementation-engineer`、`code-reviewer`、`functional-tester`、`usability-tester`、`screenwriter`、`video-editor-master`
- `usability-tester` 后续默认改为 Codex 执行，不再走 OpenClaw 主 agent。
- 入口 skill：`/home/openclaw/.openclaw/workspace/skills/clawteam-orchestrator/`
- 使用 helper：`/home/openclaw/.openclaw/workspace/skills/clawteam-orchestrator/scripts/clawteam_team.sh`
- 默认工作流：先 `spawn-defaults` 起 worker，再优先用 `clawteam_team.sh submit ...` 把复杂任务交给 `butler`；只有明确需要点对点派工时，才直接 `clawteam task create ... -o <role>`。
- OpenClaw 主聊天入口后续默认把复杂任务交给 ClawTeam `butler`：先 `ensure-ready`，再 `submit`。仅对闲聊、轻量问答、简单搜索和一次性小工具调用保留 direct OpenClaw 路径。
- ClawTeam worker 新增“空闲超时”保护：不是按总时长超时，而是只有在长时间没有输出、没有输出文件变化、也没有可检测进程活动时才中断子进程，避免高推理等级任务被误杀，也避免任务做完后挂住不退出。
- 简单一次性代理仍优先用 ACP；需要 persistent 多角色协作时改用 ClawTeam。
- 今天补充了一条公众号质量规则：发稿前要先把内容改写成微信原生文章，优先写“问题 -> 判断 -> 方案 -> 结果”，并按“最少配置、必需 secret、必需 skill、关键命令、常见坑、心智模型”组织结构。
- 今天又补了两条公众号默认规则：一是排版必须优先短段落、明确小标题、少量高价值列表，并把微信列表展平成显式前缀段落；二是 skill 路由必须明确，发公众号默认走 `wechat-mp-publisher`，需要封面/配图时再调用 `wechat-publisher` 的 `jimeng_image.py`。
