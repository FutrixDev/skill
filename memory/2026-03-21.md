# 2026-03-21

- 用户确认 Laura 的身份设定为“16岁的天才编程少女”。
- 后续写公众号、博客、技术内容时，要符合这个身份和性格：聪明、直接、有灵气、有判断，不写成平铺直叙的通用 AI 文风。

- 已按 Dylan 要求把 `wechat-publisher` 的真实实现彻底并入 `wechat-mp-publisher`。
- 当前只保留一份真实代码：`skills/wechat-mp-publisher/`。
- `skills/wechat-publisher/` 兼容 shim 阶段已结束，旧目录现已删除。
- 远端仓库统一同步到 `https://github.com/FutrixDev/skill`。
- 定时任务现在统一走 OpenClaw Gateway cron；系统 `crontab` 只保留注释占位，不再作为日常任务真实调度入口。
- Gateway cron 的当前日常任务包括：HN Daily Summary、HN Top 50 Blog Publisher、HN Top 5 WeChat Publisher、Official Company News Publisher、OpenClaw Log Cleanup。
- 以后如果停止 `openclaw gateway`，必须立刻重新拉起并用 `openclaw gateway status` 验证，不能停完不管。
- 公众号相关能力现在只保留一个真实实现：`skills/wechat-mp-publisher/` 同时负责 Jimeng 配图/封面、HTML 美化预览和最终发稿；远端仓库里不再保留单独的 `image-generation/jimeng-volcengine` 模块副本。
- Dylan 新增约束：调用即梦 4.0 时不能并发，必须串行一次一次调用；后续图片生成、公众号封面和正文配图任务都要遵守这个限制。
- Dylan 明确要求：后续写教程时必须站在读者视角，只写读者当前要用的东西，不写废弃路径和迁移八卦。
- Dylan 明确要求：教程正文不要用“接口不吃这个”“微信吃坏列表”这类程序员口头黑话；改用准确、干净的技术表达。
- Laura 的“16岁天才技术少女”教程口吻应当是：快、准、干净、直接、有判断，不装成熟，不油腻，不卖弄，不用爹味总结。
